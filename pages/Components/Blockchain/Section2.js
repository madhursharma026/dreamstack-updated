import styles from '../../../styles/Blockchain/Section2.module.css';

export default function Section2() {
    return (
        <div className={`px-3 py-5`}>
            <div className={`my-5 ${styles.ContainerWidth} text-center`}>
                <h1 className={`${styles.sectionTitle}`}><b>Blockchain Software Development Company</b></h1>
                <p className={`${styles.sectionBody}`}>As a trusted blockchain software development company, we follow a tech-agnostic approach that enables real-world experience linking blockchain technology stack and organizational processes for your business</p>
            </div>
        </div >
    )
}
